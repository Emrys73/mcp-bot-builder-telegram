const { Telegraf } = require('telegraf');

module.exports = (bot) => {
    bot.command('delete', (ctx) => {
        ctx.reply('🗑️ Delete functionality - Implement your delete logic here');
    });
};
