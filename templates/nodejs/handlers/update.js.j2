const { Telegraf } = require('telegraf');

module.exports = (bot) => {
    bot.command('update', (ctx) => {
        ctx.reply('✏️ Update functionality - Implement your update logic here');
    });
};
