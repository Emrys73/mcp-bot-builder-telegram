const { Telegraf } = require('telegraf');

module.exports = (bot) => {
    bot.command('reminder', (ctx) => {
        ctx.reply('⏰ Reminder functionality - Implement your reminder logic here');
    });
};
