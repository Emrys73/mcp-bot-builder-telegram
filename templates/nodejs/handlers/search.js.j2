const { Telegraf } = require('telegraf');

module.exports = (bot) => {
    bot.command('search', (ctx) => {
        ctx.reply('🔍 Search functionality - Implement your search logic here');
    });
};
