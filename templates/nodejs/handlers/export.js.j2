const { Telegraf } = require('telegraf');

module.exports = (bot) => {
    bot.command('export', (ctx) => {
        ctx.reply('📤 Export functionality - Implement your export logic here');
    });
};
