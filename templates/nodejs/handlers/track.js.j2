const { Telegraf } = require('telegraf');

module.exports = (bot) => {
    bot.command('track', (ctx) => {
        ctx.reply('📊 Track functionality - Implement your tracking logic here');
    });
};
